services:
  - type: web
    name: digross-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:10000 app:app & gunicorn --bind 0.0.0.0:10001 app_staff:app & wait
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
